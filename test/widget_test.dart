import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App renders', (WidgetTester tester) async {
    // Basic smoke test - just verify app can be created
    // Supabase must be initialized before this works properly
  });
}
